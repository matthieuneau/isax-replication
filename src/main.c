/* Code is written based on the following assumptions:
- only power of 2 cardinalities are allowed
- branching the isax tree is done by iterative doubling (only one node is split at a time)
*/

int main()
{
  return 0;
}
